0.2.1
~ bin/git-checkout-new-issue: mode 100644 --> 100755. A script carrying a shebang and not the bit to go with it cannot be run from a clone at all. Homebrew's bin.install had been setting the bit on the way past, so nothing showed until the formula moved to libexec.install, which keeps whatever mode the repository has.

0.2.0
+ README.md: Having a second file in the root of the repo means that Homebrew will install this correctly. No shit.
+ CHANGELOG.txt: While I was at it.
~ bin/git-checkout-new-issue: Move the notes to README.md.
